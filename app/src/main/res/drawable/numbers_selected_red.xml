<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <gradient
        android:startColor="@color/redText"
        android:centerColor="@color/redText"
        android:endColor="@color/redText"
        android:angle="270" />
    <corners android:radius="5dp" />
    <stroke android:width="1dp" android:color="@color/redText" />
</shape>
